plugins {
    // The common convention plugin that provides shared build configuration.
    id 'no.njm.java-common-conventions'

    // The java-library plugin for API and implementation separation.
    id 'java-library'
}
